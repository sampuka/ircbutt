#include <string>
#include <thread>
#include "MSocket.hpp"

class Instance
{
public:
    Instance();
    ~Instance();
    //Instance(std::string _login_name, std::string _login_OAuth);
    void login();
    void join(std::string channel);
    
private:
    void main_loop();
    std::thread *loop;
    std::string login_name;
    std::string login_OAuth;
    MSocket *sock;
};
